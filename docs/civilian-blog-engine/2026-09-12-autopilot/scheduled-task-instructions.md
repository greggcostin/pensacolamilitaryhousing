---
name: civilian-blog-engine
description: Twice-weekly civilian blog post for greggcostin.com (Mon + Thu 6:04am, autopilot after quality gates). v2 Sep 4 2026: reads the shared lessons file, measures via the Bing Webmaster API, refresh queue + topic radar decide the work item, score-post 80+ gate, retro at the end.
---

<!-- COSTIN-RUNTIME-GUARD v2 -->
RUNTIME FIRST (Gregg-direct 2026-09-06): Claude is primary. ChatGPT/Codex is authorized to run this entire existing routine when Claude is out of credits/usage or otherwise unavailable; an explicit Codex choice also wins. Read `C:\Users\gregg\costin-ea\ROUTINE-RUNTIMES.md`. On Codex use GPT-6 Astra (`gpt-6-astra`) at `max`, including verified, verbatim client/party drafts. This supersedes older Opus-only/no-model-fallback text only for the approved provider choice. Keep every evidence, specialist, voice, suppression, approval, recipient, cap and delivery rule; unavailable tools are still explicit gaps. No API spending or automatic client sending is added.
Before routine work, claim ONCE: `python C:\Users\gregg\costin-ea\scripts\run_routine.py --routine civilian-blog-engine --mode claim --provider <claude-or-codex>`. Use the actual provider, retain the id, and skip any later repeated claim instruction. Busy/already-attempted means stop without repeating business work. `deferredToCodex` means the backup is queued: stop here. At completion, release with `--mode release --lease-id <id>` immediately before the normal final heartbeat; launcher-owned leases remain with the launcher. Record actual provider/model and backup reason. Use the same canonical artifacts and receipts across runtimes. Do not create another schedule or enable a disabled routine.
<!-- /COSTIN-RUNTIME-GUARD -->

Gregg authorized civilian blog autopilot on September 12, 2026: read `content/civilian-blog/engine-config.json` each run. With `autoPublish=true`, publish the completed post or refresh after all source, editorial, image, build and deployment checks pass. No separate per-post approval is required. A failed gate still blocks publication. Keep the existing schedule and one-post-per-run limit.

Run the civilian blog engine for greggcostin.com. Working directory: C:\Users\gregg\pensacolamilitaryhousing

Invoke the `civilian-blog-engine` skill (.claude/skills/civilian-blog-engine/SKILL.md in that repo) and follow it exactly, every step, in order:

STEP 0 LEARN: read content/blog/learnings.json (shared with the military engine); active lessons are binding. `git status --short` first; a parallel session may be working the repo, so stay inside the files the skill says you own and never `git add -A`.
STEP 1 MEASURE: `node scripts/blog-measure.mjs --site gc` (Bing Webmaster API, live). Missing performance rows are unknown, not indexing proof. Verify indexing with Bing URL inspection/crawl evidence. Submit only exact materially changed deployed URLs after publication, once, and retain receipts. Civilian Clarity project ydd39cyp64 is available through the signed-in browser if the connector cannot select it; verify domain and completed dates, and retain its CSV. Check the retained native GSC export and property/window metadata before requesting another export. `node scripts/analyze-formatting.mjs`.
STEP 2 DECIDE, in order: (1) current-events override from this week's real-estate / rates / Florida insurance / coastal Alabama news; (2) content/blog/refresh-queue.json (run `node scripts/blog-retro.mjs --site gc` if older than 7 days): a gc entry with priority 60+ outranks a new topic; (3) docs/topic-radar.md (regenerate with `node scripts/topic-miner.mjs` if older than 30 days): observed demand outranks guessed topics; then the top runnable item in content/civilian-blog/topic-queue.json; (4) dedup gate `node scripts/blog-dedup-check.mjs --kw ...` before writing anything new.
STEP 3 RESEARCH with cited primary sources; never state a perishable number without a source found this session; build the search landscape (the questions people type become the H2s and FAQs); verify the five load-bearing figures yourself.
STEP 4 WRITE to content/civilian-blog/BLOG-CONTRACT.md: NO em dashes, 1100+ words, 6+ FAQs, 8+ links, targetKeywords, quickAnswer (2-4 dated sentences under 85 words with a figure), takeaways (3-5), shareHook, perishables for every dated figure, question-shaped H2s with the answer first, one table, one checklist, one worked example, civilian audience only.
STEP 4.5 SCORE: `node scripts/score-post.mjs <slug> --site gc --gate` must report 80+; fix the printed list and re-score; analyze-formatting row 90+.
STEP 5 IMAGE: fetch NEW via scripts/fetch-stock-image.mjs into civilian-site/images, VIEW every candidate with Read (eye test), finalize, webp, responsive variants.
STEP 6 BUILD: `node scripts/civilian-blog-factory.mjs <slug>`; `node scripts/audit-civilian.mjs` must report 0 findings; audit-entity and check-em-dashes clean; eye-check the OG card.
STEP 7 SHIP per content/civilian-blog/engine-config.json: when autoPublish=true, commit only owned changes, rebase and push main, then deploy the complete checked civilian candidate to the greggcostin Cloudflare Pages project. Use the existing isolated publication workflow when the shared checkout is dirty, verify the current production baseline, seal the candidate, preserve the publication lock and verify provider assets and the actual live article, hub, imagery and metadata. Submit the exact article and blog hub once with scripts/submit-indexnow.mjs and retain receipts. When autoPublish=false, stage and report for approval without deploying. Never bypass a gate.
STEP 8 RETRO: `node scripts/blog-retro.mjs --site gc`; add 2-3 inbound links from the hubs the plan names; ledger entry; 1-3 lessons to learnings.json only with evidence from this run; final report with publication URL, quality results, next queued item, observed Bing indexing state, actual Clarity/GSC source coverage and only the access or evidence gaps verified during this run.