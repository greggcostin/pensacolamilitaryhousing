import {readFileSync} from 'node:fs';
import {FINANCIAL_GUIDES,FINANCIAL_REVIEWED} from '../content/geo/financial-guide-data.mjs';
import {DEFAULT_COSTS,RATE_REFERENCE,monthlyOwnership,priceForBudget} from '../public/tools/ownership-model.js';
import {e,RATES,addCss,mapSchema,appendFaq,faqHtml,regionLinks} from './geo-core-lib.mjs';
import {placeQuickAnswer} from './quick-answer-lib.mjs';
import {renderFinancialBlog} from './build-financial-blog.mjs';
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2}).format(n);
export const financialSourceLinks=`<p>Sources reviewed September 8, 2026: <a href="https://www.travel.dod.mil/Allowances/Basic-Allowance-for-Housing/BAH-Rate-Lookup/">DoD BAH lookup</a>, <a href="https://www.travel.dod.mil/Support/ALL-FAQs/Article/2978684/bah-eligibility/">DoD BAH eligibility</a>, <a href="https://www.va.gov/housing-assistance/home-loans/funding-fee-and-closing-costs/">VA purchase funding fees</a>, <a href="https://www.va.gov/housing-assistance/home-loans/eligibility/">VA eligibility and restoration</a>, <a href="https://www.freddiemac.com/pmms">Freddie Mac PMMS</a>, <a href="https://floridarevenue.com/property/Documents/pt107.pdf">Florida buyer property-tax guidance</a> and <a href="https://www.consumerfinance.gov/owning-a-home/prepare/figure-out-how-much-you-want-to-spend/">CFPB ownership-budget guidance</a>.</p>`;
export function rateTable(all=false){const grades=all?Object.keys(RATES.FL064):['E-4','E-5','E-6','E-7','W-1','O-1','O-3'];return `<div class="table-wrap geo-table-wrap" role="region" tabindex="0" aria-label="2026 monthly BAH table"><table><caption>2026 published monthly BAH, US dollars. Confirm individual eligibility and rate protection with finance.</caption><thead><tr><th scope="col">Pay grade</th><th scope="col">FL064 with dependents</th><th scope="col">FL064 without dependents</th><th scope="col">FL056 with dependents</th><th scope="col">FL056 without dependents</th></tr></thead><tbody>${grades.map(g=>`<tr><th scope="row">${g}</th>${['FL064','FL056'].flatMap(code=>['withDependents','withoutDependents'].map(k=>`<td>${money(RATES[code][g][k])}</td>`)).join('')}</tr>`).join('')}</tbody></table></div>`;}
export function ownershipTool(){
 const defaults={price:300000,budget:3000,bah:1863,...DEFAULT_COSTS},result=monthlyOwnership(defaults.price,defaults),price=priceForBudget(defaults.budget,defaults);
 const input=(name,label,min=0,max=1e10,step=.01)=>`<label for="own-${name}">${e(label)}<input id="own-${name}" name="${name}" type="number" inputmode="decimal" required min="${min}" max="${max}" step="${step}" value="${defaults[name]}"></label>`;
 const values={...result,remaining:defaults.budget-result.monthly,gap:result.monthly-defaults.bah,priceAtBudget:price.price};
 const tile=(key,label)=>`<div><span>${label}</span><output data-cost-result="${key}">${money(values[key])}</output></div>`;
 return `<section class="geo-guide" data-financial-tool="ownership"><h2 id="calculator">Compare a home with your monthly budget</h2><p>The starting home price and expense amounts are <strong>invented planning inputs</strong>. The ${RATE_REFERENCE.rate}% rate is a conventional national benchmark dated September 3, 2026, not a VA quote. Replace every input with your own figures. Calculations stay in your browser.</p><form data-clarity-mask="True" data-ownership-budget aria-labelledby="calculator"><div class="geo-fields">${input('price','Purchase price ($)')}${input('budget','My target total monthly housing budget ($)')}${input('rate','Annual interest rate (%)',0,100,.001)}${input('termYears','Loan term (years)',1,50,1)}${input('downPaymentPct','Down payment (%)',0,100,.01)}<label for="own-feeType">Purchase funding-fee scenario<select id="own-feeType" name="feeType"><option value="first">VA first use</option><option value="subsequent">VA subsequent use</option><option value="exempt">VA exemption confirmed with lender</option><option value="none">Non-VA loan / no VA funding fee</option></select></label><label for="own-feeFinanced">VA fee payment<select id="own-feeFinanced" name="feeFinanced"><option value="true">Finance the fee</option><option value="false">Pay fee at closing</option></select></label></div><h3>Property expenses</h3><p>Use buyer-based taxes and quotes for this address. Homeowners and wind insurance belong in one combined amount without counting overlapping coverage twice. Use a positive flood estimate when coverage applies. Zero is an explicit assumption, not verified absence of a cost.</p><div class="geo-fields">${input('annualTax','Buyer-based property taxes including assessments on tax bill ($ / year)')}${input('annualInsurance','Homeowners and wind premiums combined ($ / year)')}${input('annualFlood','Flood premium ($ / year)')}${input('mortgageInsurance','Mortgage insurance, if applicable ($ / month)')}${input('hoa','HOA or condominium dues ($ / month)')}${input('assessments','Other recurring assessments, not already counted ($ / month)')}${input('maintenance','Maintenance reserve ($ / month)')}${input('utilities','Utilities ($ / month)')}</div><h3>Compare your actual BAH</h3><p>Changing the area or grade updates the published allowance only. Your target budget and property costs remain your own inputs.</p><div class="geo-fields"><label for="own-mha">BAH area<select id="own-mha" name="mha"><option value="FL064">FL064: Pensacola</option><option value="FL056">FL056: Eglin AFB / Hurlburt</option></select></label><label for="own-grade">Pay grade<select id="own-grade" name="grade">${Object.keys(RATES.FL064).map(g=>`<option${g==='E-5'?' selected':''}>${g}</option>`).join('')}</select></label><label for="own-dependents">Dependency rate<select id="own-dependents" name="dependents"><option value="withDependents">With dependents</option><option value="withoutDependents">Without dependents</option></select></label>${input('bah','Actual monthly BAH, override if different ($)')}</div><div class="geo-results" aria-live="polite">${tile('principalInterest','Principal and interest')}${tile('piti','Loan, tax and insurance')}${tile('monthly','Total including reserves and utilities')}${tile('remaining','Target budget minus total cost')}${tile('gap','Total cost minus BAH')}${tile('priceAtBudget','Price at entered total budget')}</div><p data-price-note>Rounded down to $1,000 using the entered fixed costs. Recheck taxes, insurance and fees for each actual property.</p><p class="geo-error" role="status" data-cost-error></p></form><p>The default $300,000 example produces ${money(result.principalInterest)} principal and interest, ${money(result.piti)} loan, tax and insurance costs, and ${money(result.monthly)} total monthly costs including dues, upkeep reserve and utilities. Initial cash, prepaid costs and future increases are separate. These are mathematical illustrations, not market-wide price limits or lender decisions.</p><p>To compare your income scenarios, use the <a href="/bah-to-mortgage-guide#budget-tool">actual-income and BAH worksheet</a>. To compare two homes and export a worksheet, use the <a href="https://greggcostin.com/resources/coastal-ownership-costs">civilian ownership-cost comparison</a>.</p><noscript><p>The results above show the starting example. Editing the calculator requires JavaScript. Add principal and interest, annual tax and insurance divided by 12, then the monthly charges and reserves. Use the linked printable guide for an offline worksheet.</p></noscript></section><script type="application/json" id="ownership-bah-rates">${JSON.stringify(RATES)}</script><script type="module" src="/tools/ownership-budget.js"></script>`;
}
export function financialBody(slug){
 let body=readFileSync(new URL(`../content/geo/${slug.replace('blog/','')}.fragment.html`,import.meta.url),'utf8');
 body=body.replace('{{RATE_TABLE}}',rateTable(slug==='bah-rates')).replace('{{OWNERSHIP_TOOL}}',ownershipTool()).replace('{{REGION_LINKS}}',regionLinks()).replace('{{SOURCES}}',financialSourceLinks);
 if(/\{\{[^}]+\}\}/.test(body))throw Error('Unresolved financial guide token');return body;
}
export function rebuildFinancialGuide(html,slug){
 if(slug.startsWith('blog/'))return renderFinancialBlog(html,slug);
 const spec=FINANCIAL_GUIDES[slug];if(!spec)throw Error('Unknown financial guide');
 html=html.replaceAll('FL023','FL056');const main=html.match(/<main\b[^>]*>([\s\S]*?)<\/main>/)?.[1];if(!main)throw Error('Main absent');
 const author=main.match(/<div class="author-card">[\s\S]*?<div class="ac-updated">[\s\S]*?<\/div><\/div><\/div>/)?.[0],explore=main.match(/<!-- EXPLORE_V2 -->[\s\S]*?<!-- \/EXPLORE_V2 -->/)?.[0];
 if(!author||!explore)throw Error('Expected author or navigation block absent');
 const body=`<section data-financial-guide="${slug}">${financialBody(slug)}${faqHtml(spec.faq)}<section class="geo-guide"><h2>Build your property-specific plan with Gregg</h2><p>Bring your target area, move date and the home or type of property you are considering. We can organize the local property questions before you make an offer.</p><div class="geo-links"><a href="/contact">Plan my home search</a><a href="https://greggcostin.com/resources/coastal-ownership-costs">Compare two homes</a><a href="https://greggcostin.com/resources/seller-net-proceeds">Plan sale proceeds</a></div><p class="geo-review">Contact hours: 6 a.m. to midnight Central, daily. <a href="https://greggcostin.com/team">Gregg's professional record</a>.</p></section></section>`;
 html=html.replace(/(<main\b[^>]*>)[\s\S]*?(<\/main>)/,(_,a,z)=>a+'\n'+author.replace(/<div class="ac-updated">[\s\S]*?<\/div>/,'<div class="ac-updated">Content reviewed September 8, 2026</div>')+'\n'+body+'\n'+explore+'\n'+z);
 html=html.replace(/<title>[\s\S]*?<\/title>/,()=>`<title>${e(spec.title)}</title>`).replace(/<h1\b[^>]*>[\s\S]*?<\/h1>/,()=>`<h1>${e(spec.h1)}</h1>`).replace(/<p class="lead">[\s\S]*?<\/p>/,()=>`<p class="lead">${e(spec.lead)}</p>`);
 html=html.replace(/<meta\b[^>]*>/gi,tag=>{const key=tag.match(/(?:name|property)=["']([^"']+)["']/i)?.[1],value=['description','og:description','twitter:description'].includes(key)?spec.description:['og:title','twitter:title'].includes(key)?spec.title:key==='article:modified_time'?FINANCIAL_REVIEWED:null;return value?tag.replace(/content=["'][^"']*["']/i,()=>`content="${e(value)}"`):tag;});
 html=mapSchema(html,node=>{if(node['@type']==='Article'||node['@type']==='BlogPosting'){node.headline=spec.h1;node.description=spec.description;node.dateModified=FINANCIAL_REVIEWED;}if(node['@type']==='FAQPage')node.mainEntity=[];});html=appendFaq(html,spec.faq);
 html=html.replace(/(<p\b[^>]*data-last-updated="true"[^>]*>)[\s\S]*?(<\/p>)/,(_,a,z)=>a+'Last updated: September 8, 2026'+z);
 html=html.replace(/Content last verified: [^<]+/g,'Content last verified: September 8, 2026');
 return addCss(placeQuickAnswer(html,{text:spec.quickAnswer,date:'September 8, 2026'}));
}
