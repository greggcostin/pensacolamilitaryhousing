// Same visible professional details for React, prerendered HTML and the civilian profile.
import {IDENTITY as I} from './entityData.js';
import {BIOGRAPHY} from './biography.js';
const esc=value=>String(value).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
export function professionalProfileHtml(){
 const date=new Date(I.licenses[0].checkedOn+'T12:00:00Z').toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric',timeZone:'UTC'});
 return `<section id="professional-details" data-professional-record style="background:#141B29;color:#E8E9EB;border:1px solid #414652;padding:clamp(20px,3vw,32px);margin:32px 0;max-width:100%;overflow-wrap:anywhere"><h2 style="color:#fff;font-size:26px;font-weight:500;margin:0 0 16px">Professional details</h2><p style="font-size:16px;line-height:1.7;margin:0 0 14px"><strong>${esc(I.name)}</strong> · ${esc(I.brokerage.name)}<br>${esc(I.suffix)}</p><p data-profile-qualifications style="font-size:16px;line-height:1.7;margin:0 0 16px">${esc(BIOGRAPHY.qualifications)}</p><ul style="padding-left:20px;line-height:1.8;margin:0 0 16px">${I.licenses.map(l=>`<li><a href="${esc(l.url)}" style="color:#E3CA8B">${esc(l.state)} license ${esc(l.identifier)}</a></li>`).join('')}</ul><p style="font-size:15px;line-height:1.7;margin:0 0 16px">Office: ${esc(I.addressLine)}<br><a href="tel:${esc(I.telephone)}" style="color:#E3CA8B">${esc(I.phoneDisplay)}</a> · <a href="mailto:${esc(I.email)}" style="color:#E3CA8B">${esc(I.email)}</a></p><p style="font-size:15px;line-height:1.7;margin:0 0 16px">Contact hours: ${esc(I.contactHours.label)}.</p><p style="font-size:13px;line-height:1.6;color:#B8BAC0;margin:0 0 14px">License status checked ${esc(date)}. Use the official registry links above to confirm current status.</p><p style="font-size:14px;line-height:1.7;margin:0"><a href="${esc(I.websites[0].profile)}" style="color:#E3CA8B">Civilian buyer and seller profile</a> · <a href="${esc(I.websites[1].profile)}" style="color:#E3CA8B">Military relocation profile</a> · <a href="${esc(I.recordUrl)}" style="color:#E3CA8B">Business details (JSON)</a></p></section>`;
}
