import test from 'node:test';import assert from 'node:assert/strict';
import {sellerNet,ownershipComparison,floridaTaxEstimate} from '../civilian-site/assets/costin-guide-tools.mjs';
const sale={price:450000,payoff:285000,compensation:18000,closing:3200,buyerCredit:6000,repairs:1500,otherLiens:0,prorations:2400,moving:5000,nextPurchase:30000,reserve:40000};
test('seller example reproduces the source guide and separates next-move cash',()=>assert.deepEqual(sellerNet(sale),{deductions:316100,proceeds:133900,afterNextMove:58900}));
test('proration credits increase proceeds; an underwater sale retains the negative amount',()=>{assert.equal(sellerNet({...sale,prorations:-2400}).proceeds,138700);assert.equal(sellerNet({...sale,price:200000}).proceeds,-116100);});
test('missing, negative or non-finite costs cannot silently become zeros',()=>{for(const val of ['',null,-1,Infinity,'abc'])assert.throws(()=>sellerNet({...sale,closing:val}));});
test('two-home example retains all ownership costs and distinguishes cash outlay',()=>{const a={principalInterest:2350,tax:500,insurance:300,flood:90,mortgageInsurance:0,assessments:0,association:75,maintenance:250,utilities:225,initialCash:20000},b={principalInterest:2200,tax:280,insurance:475,flood:140,mortgageInsurance:0,assessments:0,association:450,maintenance:150,utilities:225,initialCash:25000};const r=ownershipComparison({a,b,months:36});assert.equal(r.a.monthly,3790);assert.equal(r.b.monthly,3920);assert.equal(r.monthlyDifference,130);assert.equal(r.horizonDifference,9680);assert.throws(()=>ownershipComparison({a,b,months:0}));assert.throws(()=>ownershipComparison({a,b,months:1.5}));});
test('tax example uses different taxable bases and mills per thousand',()=>assert.deepEqual(floridaTaxEstimate({schoolTaxable:400000,schoolMills:6,otherTaxable:375000,otherMills:10,nonAdValorem:300,sellerTax:3000}),{school:2400,other:3750,annual:6450,monthly:537.5,changeFromSeller:287.5}));

test('mortgage insurance and recurring assessments are included in both-home totals',()=>{const a={principalInterest:1000,tax:100,insurance:100,flood:0,mortgageInsurance:75,association:0,assessments:125,maintenance:100,utilities:100,initialCash:0};const b={...a,mortgageInsurance:0,assessments:0};const r=ownershipComparison({a,b,months:12});assert.equal(r.a.monthly,1600);assert.equal(r.b.monthly,1400);assert.equal(r.horizonDifference,-2400);assert.throws(()=>ownershipComparison({a:{...a,assessments:''},b,months:12}));});
